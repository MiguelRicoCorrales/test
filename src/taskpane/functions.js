/** 
 * add2
 * @customfunction
 * @param first First Number
 * @param second Second Number
 * @returns the sum of the numbers 
 * */
function getTMRR(first, second) {
    return first + second;
}
CustomFunctions.associate("getTMRR", getTMRR);
